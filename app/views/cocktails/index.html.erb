<div id="container" class="m-0">
  <div id="title" class="p-3">
    <h1 >Cocktails</h1>
    <%=link_to "Add a cocktail", new_cocktail_path, class: "btn btn-primary" %>
  </div>

    <div class="cards">
    <% @cocktails.each do |cocktail| %>
      <div id= card> <%= link_to cocktail.name, cocktail_path(cocktail.id), class: "text-white" %></div>
    <% end %>
    </div>
</div>
